[workspace]
members = ["katori-gui", "gdbadapter"]

[package]
name = "katori"
version = "0.1.0"
edition = "2021"

[dependencies]
katori-gui = { path = "katori-gui" }
gdbadapter = { path = "gdbadapter" }
log = "0.4"
simple_logger = "4.3"
